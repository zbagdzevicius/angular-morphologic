import { FormToValuesPipe } from './form-to-values.pipe';

describe('FormToValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormToValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
